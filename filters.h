#include "buffer.h"

int two_term_filter(struct Buffer* cbuf);
int tree_term_filter(struct Buffer* cbuf);
int four_term_filter(struct Buffer* cbuf);
int five_term_filter(struct Buffer* cbuf);
int six_term_filter(struct Buffer* cbuf);
int seven_term_filter(struct Buffer* cbuf);
